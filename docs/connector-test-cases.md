# 连接器功能测试用例集(双侧)

> 2026-09-09 基于 Playwright MCP 全量自动化测试沉淀;用于连接器功能回归验证。覆盖管理侧(官方连接器)与空间侧(自定义连接器)全部页面功能,含详情抽屉内工具/连接子功能。

## 前置条件

| 项 | 值 |
| --- | --- |
| 前端地址 | 本地 dev server(如 `http://198.18.0.1:3000`) |
| 后端 | 测试环境后端(以本地 `.env` / 代理配置为准,勿写入仓库) |
| 登录账号 | 测试账号(向团队索取,勿将真实账号密码写入仓库) |
| 管理侧入口 | `/system/connector-manage` |
| 空间侧入口 | `/space/{spaceId}/connector`(用例使用 spaceId=57) |
| 测试数据约定 | service 一律 `at_` 前缀(空间侧自动加 `s_` 前缀),**验证完成后必须全部删除** |
| 假凭证 | Api Key 用 `sk-autotest-123456`;Bearer 用 `test-bearer-token-123`;BASE URL 用 `https://api.example.com`(不可达域名,调试预期返回 upstream_error) |

---

## 一、管理侧用例(`/system/connector-manage`)

### A. 列表与搜索

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M01 | 列表展示 | 打开页面 | 表格含列:排序/连接器/service/认证方式/工具数/启用状态/更新时间/操作;虚拟滚动正常 |
| M02 | 搜索 | 搜索框输入名称或 service(如 `ably`) | 列表实时过滤,清空后恢复 |
| M03 | 虚拟滚动定位 | JS 滚动 `.ant-table-tbody-virtual-holder` 到底部 | 新建数据位于列表尾部可见 |

### B. 新增

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M04 | 必填校验 | 新增弹窗直接点「创建连接器」 | SERVICE/显示名称/BASE URL 报必填错误,不提交 |
| M05 | 创建成功 | 填 SERVICE=`at_xxx`、显示名称、BASE URL、认证方式(免鉴权)→ 创建 | toast 成功,列表出现新行;SERVICE 创建后不可改 |

### C. 编辑(行内「编辑」)

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M06 | 回显与锁定 | 打开编辑表单 | 全字段回显;SERVICE 输入框 disabled |
| M07 | 认证方式联动 | 认证方式从「免鉴权」切「Api Key」 | 联动出现认证配置:凭证字段名/注入位置(请求头、query)/请求头名称/值前缀 |
| M08 | 保存联动 | 修改显示名称 → 保存 | toast 成功;详情抽屉同步刷新为新值 |

### D. 详情抽屉(行内「查看」)— 工具管理

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M09 | 详情展示 | 打开详情 | 认证方式/BASE URL/通用代理/连接状态/工具列表(名称/ACTIONKEY/说明/状态/接口/操作) |
| M10 | 添加工具 | + 添加工具:ACTIONKEY/名称/说明/执行类型(HTTP 接口)/方法/路径/超时/响应提取路径/QUERY 参数映射/HEADER 映射/BODY 字段/输入参数 Arg 树 | 保存成功,工具行出现在列表;ACTIONKEY 创建后不可改 |
| M11 | 工具编辑 | 工具行「编辑」 | 回显完整(ACTIONKEY 锁定);修改保存成功,列表更新 |
| M12 | 工具停用/启用 | 工具行「停用」→「启用」 | 状态即时双向切换,操作列文案联动 |
| M13 | 工具调试(未建连) | 工具行「调试」→ 执行 | 返回业务错误 `connection_required` + 引导建连提示 |
| M14 | 工具调试(已建连) | 建连后再次调试执行 | 不再返回 connection_required;真实发起上游请求,假域名返回 `DNS 解析失败 / upstream_error`(带 executionId) |
| M15 | 工具删除 | 工具行「删除」→ 确认弹窗 | 删除成功,行消失 |

### E. 连接管理

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M16 | 去连接 | 详情「去连接」→ 连接设置弹窗,填 APIKEY 凭证 → 加密保存并建立连接 | 状态变「已连接」,按钮变「断开连接」 |
| M17 | 断开连接 | 「断开连接」→ 确认气泡「确认断开」 | 状态变「未连接」 |

### F. 启停 / 导出 / 删除

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| M18 | 连接器启停 | 行内「停用」→「启用」 | 状态双向切换 |
| M19 | 导出 | 行内「导出」 | 下载 `.connector.json`;内容含 `source: nuwax`、providers[](service/displayName/authType/authConfig/actions 与实际一致) |
| M20 | 删除连接器 | 行内「删除」→ 确认弹窗(含「工具一并删除/有连接会被拒」提示)→ 删除 | 删除成功,列表消失;若未断连删除被拒(需先断开) |

---

## 二、空间侧用例(`/space/{spaceId}/connector`)

### A. 列表与搜索

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| S01 | 列表展示 | 打开页面 | 比管理侧多「连接状态」列;空间切换器正常 |
| S02 | 搜索 | 搜索框输入名称/service | 实时过滤 |

### B. 新增 / 查看

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| S03 | 必填校验 | 新增弹窗直接提交 | 必填报错,不提交 |
| S04 | 新增(S\_ 前缀) | SERVICE=`at_xxx` → 创建 | 实际 service 为 `s_at_xxx`(自动加 S\_ 前缀) |
| S05 | 查看详情 | 行内「查看」 | 详情弹窗展示认证方式/BASE URL/通用代理/连接状态/工具列表 |

### C. 连接管理

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| S06 | 去连接(Bearer) | 认证方式 Bearer → 去连接,填 TOKEN → 保存 | 状态变「已连接」 |
| S07 | 断开连接 | 「断开连接」→ 确认气泡 | 状态变「未连接」 |

### D. 工具管理(详情内)

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| S08 | 添加工具 | 同 M10 | 创建成功 |
| S09 | 工具编辑 | 工具行「编辑」改名称 → 保存 | **接口 `PUT /api/connector/actions/{id}` 返回 200**,弹窗关闭、列表更新(历史缺陷:POST 返回 405,已修复,见下) |
| S10 | 工具停用/启用 | 工具行「停用」→「启用」 | 状态双向切换 |
| S11 | 工具调试(已建连) | 工具行「调试」→ 执行 | 真实发起上游请求,假域名返回 `DNS 解析失败 / upstream_error`(带 executionId) |
| S12 | 工具删除 | 工具行「删除」→ Popconfirm「确认删除」 | 删除成功,表格「暂无数据」 |

### E. 编辑 / 启停 / 导出 / 删除

| # | 用例 | 步骤 | 预期 |
| --- | --- | --- | --- |
| S13 | 编辑连接器 | 行内「编辑」改显示名称/标签 → 保存 | 保存成功,详情联动 |
| S14 | 连接器启停 | 行内「停用」→「启用」 | 状态双向切换 |
| S15 | 导出 | 行内「导出」 | `.connector.json` 含 actions 定义,与实际一致 |
| S16 | 删除连接器 | 行内「删除」→ 确认弹窗 → 删除 | 删除成功;未断连时删除被拒(需先断开) |

---

## 三、已知问题与历史缺陷

| 状态 | 优先级 | 问题 | 备注 |
| --- | --- | --- | --- |
| **已修复(2026-09-09)** | P1 | 空间侧工具编辑保存 `POST /api/connector/actions/{id}` 返回 405,后端仅接受 PUT | 修复:`src/services/systemManage.ts` `apiConnectorActionUpdate` 改 PUT;回归用例 S09 |
| 人工确认保留 | P2 | 管理侧搜索结果状态下整行按钮 disabled,行内 查看/编辑/导出/启停/删除 不可点击,需清空搜索后操作 | 疑似拖拽排序禁用逻辑波及操作列;空间侧无此问题 |
| 人工确认保留 | P3 | 排序按钮 disabled 态 aria-label 显示未翻译 i18n key `PC.Pages.SystemConfigI18n.columnSortDisabled` | 正常态显示「排序」 |
| 人工确认保留 | P3 | console 预存警告(antd Select dropdownRender/onDropdownVisibleChange 废弃、virtual table scroll.y must be number、useForm not connected、Function components cannot be given refs、div 作为 tr 子元素) | 非阻断 |
| 待人工 | — | 认证方式/启用状态/连接状态三个筛选下拉(双侧)自动化不稳定,未能自动验证筛选逻辑 | 建议人工点验 |

## 四、回归提示

- 优先回归 **S09**(刚修复的 PUT 链路),再做双侧增删改查主链路
- 长列表(管理侧 ~400+ 连接器)虚拟滚动下,新数据在尾部,需 JS 滚动 `.ant-table-tbody-virtual-holder`
- 管理侧行是整行 button 包裹,自动化点击行内操作时注意定位精确性(文字节点而非整行)
- 每轮测试结束删除所有 `at_` / `s_at_` 前缀数据(删除连接器前先断开连接)
