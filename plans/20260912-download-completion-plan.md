# 商业客户端文件下载完成反馈

依据 Nuwax 客户端提测收尾计划：导出触发 anchor 不代表下载完成。商业客户端复用现有 native.saveImage 文件保存桥（保留接口名），等待原子写入成功才提示成功；取消不重试、不提示成功；原生失败显示错误。普通浏览器仍交由原生下载 UI，不宣称文件落盘成功。图片另存失败显示原生错误。壳拒绝把 HTTP 200 JSON/HTML 错误体保存成 zip/图片。覆盖等待、取消、错误、浏览器降级，补类型及构建检查。线上正式发布不在本轮范围。
