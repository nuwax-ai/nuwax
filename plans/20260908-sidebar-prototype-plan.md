# 实施计划：项目任务列表原型对齐

- 对应 spec：specs/sidebar-prototype.md
- 状态：已完成（样式与交互；既有测试及接口限制如下）

## 改动文件清单
- NewHomeSection：分组键盘操作、批量展开入口、隐藏保留状态。
- ProjectPanel：批量展开方法、项目图标/菜单按钮、行和子级样式。
- ConversationItem：单栏 compact 变体及悬停样式。
- locales：批量展开/收起文案。

## 实施顺序
先记录原型与规格，再修改组件，格式化和针对性测试，最后浏览器验收。

## 证明成立的测试
组件验证批量折叠、菜单点击不触发展开；运行相关已有回归。

## 风险与回退
项目子任务尚无后端接口；只验证可提供的真实数据与隔离组件场景。回退上述组件修改即可。

## 验证结果
- 侧栏测试：3 文件 / 11 项通过；项目交互用例最终复跑 2 项通过。
- 浏览器 localhost:3000：单栏紧凑列表、项目批量收起、更多菜单不改变折叠态、分组隐藏/恢复保持折叠状态、Enter 展开、任务跳转与选中背景均通过。
- TypeScript 全库仍有既有错误；NewHomeSection 与本次新增测试路径没有诊断。
- 额外会话回归：51 文件通过 / 1 文件失败，486 项通过 / 1 项失败；conversationInfoModel 云电脑 gate 期望 apiEnsurePod(1001)，实际为 (1001, undefined)，本次未修改该模型及测试。
- 旧 utils.test.ts 被 Umi 传递导入造成的 TextEncoder 环境异常阻断（0 用例），不计为通过。
- 项目 children 接口缺失，因此真实子任务内容尚不能端到端验收；只完成对应样式。
- 工作期间另一个任务在 ProjectPanel 及 AppDevPro / SpaceProjectManage 接入真实项目操作；已保留并行改动，不归属本次样式工作。

## Hover 补充（用户截图）
- 项目行 hover 显示加号和更多按钮，保留折叠箭头。
- 子任务 hover 使用半透明白色圆角背景，右侧白色操作浮层覆盖时间，仅浮层有轻阴影。按钮 hover 使用主题色浅底，提示“添加对话”。
- 加号当前明确提示项目添加对话未接入，不创建虚假数据。

## 首页子类型展示补充
按用户原型将 ChatBoxRecommendNav 改为单行胶囊横向滚动，左右边界按钮与渐隐随滚动更新，分类变化复位，监听容器尺寸变化，支持减少动态效果偏好。
