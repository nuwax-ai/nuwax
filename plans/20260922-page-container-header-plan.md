# page-container 标题栏统一接入计划

## 实施顺序

1. 新增 `PageContainerHeader` 公共组件及回归测试。
2. 以 `SpaceMcpManage` 为基准页接入，验证宽屏单行、窄屏换行右对齐。
3. 接入结构同构的工作空间资源列表页（项目、网页应用、插件、工作流、知识库、数据表、模型等）。
4. 接入系统管理与更多页面中的页面级标题栏。
5. 按审计清单处理详情页；不符合公共契约的页面记录例外，不强行套壳。

## 验证门

- 公共组件测试与接入页面相关测试通过。
- 生产构建通过。
- 1240px Windows 真机无横向溢出，操作区换行后仍贴右。
- 1900px 浏览器保持参考图的单行布局。
- 不改动用户现有会话渲染 WIP。
