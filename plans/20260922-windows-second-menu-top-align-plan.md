# Windows 二级菜单顶部对齐计划

## 目标

- Windows 沉浸客户端内，二级菜单背景从原生顶栏下方开始，与 `page-container` 顶边对齐。
- 一级会话侧栏保持现有整高背景和顶栏避让。
- 浏览器、独立窗口与 macOS 现有布局不变。

## 实现

1. 抽取二级菜单壳层几何策略，Windows/Linux 使用 `CONTENT_TOP` 作为外部退让。
2. 用剩余的 `TOP - CONTENT_TOP` 作为内部顶距，保持菜单内容原有纵向位置。
3. 同步扣减二级列高度，避免底部溢出。
4. 补纯函数回归测试并做 Windows 真机验证。
